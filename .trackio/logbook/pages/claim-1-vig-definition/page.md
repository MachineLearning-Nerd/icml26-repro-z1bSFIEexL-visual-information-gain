# Claim 1 — VIG definition toy

A fixed-probability CPU toy implements the source definition `log(PPL(A|Q)/PPL(A|Q,I))`, equivalently the mean token cross-entropy difference. Matching, partial, conflicting, and weak-visual controls show positive, smaller-positive, negative, and near-zero VIG respectively. This is **toy** evidence only: no VLM, blurred image, MS-COCO sample, or benchmark is executed.
